Telegram VPN бот с системой подписок
====================================

Бот для управления VPN подписками через x-ui/3x-ui панель с системой оплаты, реферальной программой и управлением пользователями.

Основные возможности
-------------------

- 🔐 Автоматическое создание VPN подключений (VLESS)
- 💰 Система подписок с различными тарифами
- 💳 Управление балансом и оплатой
- 🎁 Реферальная программа с бонусами
- 📊 Статистика и управление пользователями
- 👨‍💼 Админ-панель для управления платежами

Быстрый старт
-------------

1) Установите зависимости:

```bash
python3 -m venv .venv
source .venv/bin/activate  # На Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

2) Настройте переменные окружения:

```bash
cp .env.example .env
# Заполните .env файл:
# - BOT_TOKEN - токен Telegram бота
# - ADMIN_IDS - ID администраторов (через запятую)
# - XUI_BASE_URL - URL панели x-ui/3x-ui
# - XUI_USERNAME/XUI_PASSWORD или XUI_API_TOKEN - авторизация
# - XUI_INBOUND_ID - ID inbound для VLESS
# - DB_PATH - путь к файлу базы данных (по умолчанию vpn_bot.db)
# - REFERRAL_BONUS - бонус за реферала (по умолчанию 50.0)
# - MIN_PAYMENT - минимальная сумма пополнения (по умолчанию 100.0)
```

3) Запустите бота:

```bash
python -m bot.main
```

Использование
-------------

### Команды для пользователей:

- `/start` — регистрация и приветствие
- `/subscription` или кнопка "📦 Моя подписка" — информация о текущей подписке
- `/buy` или кнопка "🛒 Купить подписку" — покупка подписки
- `/balance` или кнопка "💰 Баланс" — информация о балансе
- `/payment` — пополнение баланса
- `/invite` или кнопка "🎁 Инвайт" — реферальная ссылка
- `/stats` или кнопка "📊 Статистика" — статистика пользователя

### Команды для администраторов:

- `/admin` — админ-панель со статистикой всех пользователей

### Тарифные планы:

1. **Базовый** - 30 ГБ / 30 дней - 199₽
2. **Стандарт** - 100 ГБ / 30 дней - 399₽
3. **Премиум** - 300 ГБ / 30 дней - 799₽
4. **Безлимит** - безлимитный трафик / 30 дней - 1299₽
5. **Недельный** - 50 ГБ / 7 дней - 149₽
6. **Месячный** - 200 ГБ / 90 дней - 999₽

### Реферальная программа:

- Каждый пользователь получает уникальную реферальную ссылку
- При регистрации нового пользователя по ссылке, пригласивший получает бонус (по умолчанию 50₽)
- Бонусы начисляются на баланс автоматически

### Система оплаты:

- Пользователь создает заявку на пополнение баланса
- Администратор подтверждает платеж через админ-панель
- После подтверждения баланс пополняется автоматически
- Подписка покупается с баланса автоматически

База данных
-----------

Бот использует SQLite базу данных со следующими таблицами:

- `users` - пользователи бота
- `subscriptions` - подписки пользователей
- `payments` - платежи
- `invites` - реферальные коды

База данных создается автоматически при первом запуске.

Важно про x-ui API
------------------

Форк x-ui/3x-ui может иметь разные пути и метод авторизации. В коде реализована поддержка:
- Авторизации через API токен (XUI_API_TOKEN)
- Авторизации через логин/пароль (XUI_USERNAME/XUI_PASSWORD)

Если ваш форк использует иные пути/параметры — обновите `bot/xui_client.py` под свою панель.

Структура проекта
-----------------

```
bot/
├── main.py                  # Основной файл бота с обработчиками
├── config.py                # Конфигурация и загрузка настроек
├── database.py              # Работа с базой данных
├── subscription_service.py  # Сервис управления подписками
└── xui_client.py            # Клиент для работы с x-ui API
```

Разработка
----------

Для разработки рекомендуется использовать виртуальное окружение:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Запуск в режиме разработки:

```bash
python -m bot.main
```
